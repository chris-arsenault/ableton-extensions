node_modules/
dist/
*.tsbuildinfo
*.ablx
.DS_Store

# Ableton Extensions SDK — CLOSED BETA, not redistributable. Neither the Centercode
# download nor the packages extracted from it may be committed. Populate vendor/
# locally from your own beta access (see vendor/README.md).
extensions-sdk-1.0.0-beta.0.zip
vendor/*.tgz

# Local credential cache written by the auth client (never commit real tokens)
.sulion/
credentials.json

# Local env overrides
.env
.env.local
